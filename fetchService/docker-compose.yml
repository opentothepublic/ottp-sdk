version: '3.8'
services:
  app:
    image: ottp-fetchservice
    build:
      context: ./
      target: dev
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
    ports:
      - "8000:8000"